<h1>Simple Chat</h1>

<ul id="chat">
    <%= render @messages %>
</ul>

<%= form_for @message, remote: true do |f| %>
    <%= f.text_field :content %>
    <%= f.submit "Send" %>
<% end %>

<%= subscribe_to "/messages/new" %>
